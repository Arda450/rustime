# Code-Snippets (nur zum Lesen)

Diese Dateien werden **nicht** vom Projekt gebaut. Sie zeigen Muster aus Rustime in Minimalform.

Typ-Check (optional): `npx tsc -p docs/rustime-lernmaterial/examples` (nutzt `stubs.d.ts`, nicht `tauri-app/node_modules`).

| Datei | Thema |
|-------|--------|
| `react-state.tsx` | useState, Revision, invoke |
| `tracking-state.rs` | Arc, Mutex, AtomicBool |
| `tauri-command.rs` | Command + State |
| `tauri-event.tsx` | emit / listen |
